name: Simple GitHub Actions Demo

on: [push]

jobs:
    demo-job:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Print Hello Message
              run: echo "Willkomen zum Kurs 'GitHub für Entwickler' bei der GFU!"